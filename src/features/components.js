export const ImageName = (filename, coords) => { return {filename: filename, coords: coords}}
export const Person = (id, name, email, phoneNumber, imageNames) => { return {id: id, name: name, email: email, phoneNumber: phoneNumber, imageNames: imageNames}}
export const ImageFile = (name, url) => { return {name: name, url: url}}

// loading spinner
